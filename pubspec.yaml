name: moor_local_database
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.1
  moor_flutter: ^3.1.0

  sqlite3_flutter_libs:
  path_provider:
  path:

  # for slide to delete
  flutter_slidable: ^0.5.7
  provider: ^4.3.2+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  moor_generator: ^3.1.0
  build_runner:

flutter:
  uses-material-design: true

  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

